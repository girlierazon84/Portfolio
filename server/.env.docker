# server/.env.docker  (can be committed, NO SECRETS HERE)

PORT=5000

# MongoDB service inside Docker network (see docker-compose.yml -> service name "mongo")
MONGO_URI=mongodb://mongo:27017/portfolio_db
DB_NAME=portfolio_db

# Admin credentials are NOT stored here to avoid leaking secrets.
# Set them via docker-compose or your hosting provider's env settings:
# ADMIN_USER=<your-admin-username>
# ADMIN_PASS=<your-admin-password>
#
# Example in docker-compose:
#   environment:
#     - ADMIN_USER=girlie84
#     - ADMIN_PASS=${ADMIN_PASS}
